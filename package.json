{
  "name": "express-asynchronous-api",
  "description": "make express endpoint to asynchronous api",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test",
    "lint": "eslint src/** --fix",
    "release": "npx standard-version --no-verify && git push --follow-tags origin main",
    "format": "npx prettier -w src"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "express": "^5.0.0",
    "ioredis-mock": "^8.9.0",
    "uuid": "^11.0.0"
  }
}
